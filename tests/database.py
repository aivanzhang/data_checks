from src.data_checks.database.utils import *
from sqlalchemy import create_engine

# set_engine(create_engine("sqlite:///:memory:"))
# print(is_engine_defined())
# init_database("sqlite:///:memory:")
# print(is_engine_defined())
